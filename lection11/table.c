#include <stdio.h>
#include <stdbool.h>

#define N 100

typedef struct table
{
    int data;
    int key;
}
table;

table arr[N];

int main(void)
{
    int i = 0;
    while(i < N && !isEqual(arr[i].key, k))
}